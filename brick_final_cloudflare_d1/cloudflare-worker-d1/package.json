{
  "name": "brick-classroom-cloudflare-d1",
  "version": "4.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "schema:local": "wrangler d1 execute brick_history --local --file=./schema.sql",
    "schema:remote": "wrangler d1 execute brick_history --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}